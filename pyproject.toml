[project]
name = "myelin-backend"
version = "0.1.0"
description = "Myelin backend MVP: deterministic CEO decision-simulation platform for business education"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "uvicorn[standard]>=0.51.0",
]
